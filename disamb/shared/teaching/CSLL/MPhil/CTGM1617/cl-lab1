% Jan 28, 2016

0) run putty.exe (from the bottom left of the computer)

1) enter macneill.scss.tcd.ie
   choose ssh (the default)

   (from a machine like an apple, attached to the College
   network, in a terminal type: ssh -l CollegeUsername macneill.scss.tcd.ie)

2) let n = the right most digit of your birthdate

   ssh lg12ln 

3) ls
   ls -l
   ls -lat

4) mkdir CTGM

5) ls
   ls -l
   ls -lat

6) emacs filename.pl
   type "%stuff"
   ctrl-a
   ctrl-k
   ctrl-y
   <return>
   ctrl-y
   <return>
   ctrl-y
   <return>
   ctrl-x s
   ctrl-x ctrl-c

7) ls
   ls -l
   ls -lat

8) cp filename.pl newfilename.pl

9) ls
   ls -l
   ls -lat

10) emacs newfilename.pl

11) type:

element(First,[First|_]).
element(First,[_|Rest]) :-
		   element(First,Rest).

12) ctrl-x s

13) Esc-X

14) run-prolog

15) consult(newfilename).

16) listing.

17) element(a,[c,b,a]).

18) element(X,[c,b,a]).

type ; after each ?

19) halt.

20) ctrl-x 0

21) ctrl-x ctrl-c

22) exit
(lg12ln)

23) exit
(macneill)