#!/usr/bin/perl

use warnings;
use strict;
use utf8;

binmode(STDIN, ":utf8");
binmode(STDOUT, ":utf8");

my %wordsmap = (
  'hˈeːn' => 'F EY N, H EY N',
  '' => '',
);

my %phonmap = (
    'm' => 'M',
    'ɐ' => 'UH',
    'A' => 'AO',
    '
);

while(<>) {
    
}


